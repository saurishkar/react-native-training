import {StyleSheet} from 'react-native';

const FormElementStyles = StyleSheet.create({
  textInput: {
    color: 'blue',
    fontWeight: 'bold',
    fontSize: 30,
  },
});

export const {textInput: textInputStyle} = FormElementStyles;
